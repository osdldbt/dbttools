Installing dbttools:

    cmake -H. -DCMAKE_INSTALL_PREFIX=/path
    make install

This will put all binaries in '/path'.

-----

Uninstalling dbttools:

    xargs rm < install_manifest.txt

The file 'install_manifest.txt' will be created after running 'make install'.
